services:
  api:
    ports:
      - "8000:8000"
    # Dev-only: run the live source tree so code edits need just a restart,
    # not a full image rebuild. (Local only — not used in the prod build.)
    volumes:
      - ./backend/app:/app/app
